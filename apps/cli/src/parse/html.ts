// Re-export from @squirrelscan/parser (canonical source)
export {
  parsePage,
  extractMeta,
  extractH1,
  extractOG,
  extractTwitter,
  extractLinksBasic as extractLinks,
  extractImages,
  extractSchema,
  extractHeadings,
  extractContent,
  type ParsedPage,
} from "@squirrelscan/parser";
