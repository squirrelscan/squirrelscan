// Moved to @squirrelscan/fetchers so the cloud container can share it (#294).
export {
  createHybridDocumentFetcher,
  defaultShouldUpgrade,
  type HybridFetcherOptions,
} from "@squirrelscan/fetchers";
