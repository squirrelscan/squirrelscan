# squirrelscan

This package installs the `squirrel` CLI binary for macOS, Linux, and Windows.

```bash
npm install --global squirrelscan
squirrel audit https://example.com
```

The CLI source is available in [`apps/cli`](https://github.com/squirrelscan/squirrelscan/tree/main/apps/cli) under the MIT License.
